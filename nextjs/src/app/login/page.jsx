import React from 'react'
import LoginPage from '@/components/LoginPage'

export const metadata = {
    title: 'login',
    description: '...',
}

const page = () => {
    return (
        <div><LoginPage /></div>
    )
}

export default page